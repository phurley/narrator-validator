commands:
  - id: command.investigate
    name: Investigate
    parameters:
      - name: target
        types: [entity]
        min: 1
        max: 1
        candidates: { from: [current_location] }
